# ContextECF Fabric Starter — Environment Configuration
# Copy this file to .env and replace all REPLACE_ME values before running.

# Registry credentials (provided after trial signup)
REGISTRY=REPLACE_ME
FABRIC_VERSION=REPLACE_ME

# Database password (auto-generated by install.sh if not set)
POSTGRES_PASSWORD=REPLACE_ME
